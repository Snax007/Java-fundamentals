// Traditional for loop
int[] numbers = {10, 20, 30, 40, 50};
for (int i = 0; i < numbers.length; i++) {
    System.out.println(numbers[i]);
}

// Enhanced for loop (cleaner!)
for (int num : numbers) {
    System.out.println(num);  // num takes each value: 10, 20, 30, 40, 50
}
